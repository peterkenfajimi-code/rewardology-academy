# Repository cleanup backlog

Small follow-up items that do not block current company publishes.

- **Safaricom tier2 retrofit (Kenya):** Safaricom's published `pension_administrator_type` ("NSSF + private occupational scheme") predates migration 011's `tier2_employer_contribution_pct` / `tier2_employee_contribution_pct` fields. Retrofit disclosed Tier 2 rates into those fields when source review confirms them, for consistency with Ghana's multi-tier representation.

- **GCB Guide-1-only scope (Ghana):** GCB Bank PLC was collected deliberately from a single Guide 1 source (2024 annual report) to validate Ghana's three-tier pension structure — not a pipeline failure. The thin public profile (retirement-heavy, empty Health/Risk/Leave sections) reflects "not yet collected from Guides 2–6," not missing data from the one document that was processed. Extend via Guide 2 (careers page) in a dedicated session with its own validation target — same pattern as GTCO careers reconciliation — rather than folding into the Guide 1 proof-point work.
