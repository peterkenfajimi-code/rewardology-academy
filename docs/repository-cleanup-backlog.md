# Repository cleanup backlog

Small follow-up items that do not block current company publishes.

- **Safaricom tier2 retrofit (Kenya):** Safaricom's published `pension_administrator_type` ("NSSF + private occupational scheme") predates migration 011's `tier2_employer_contribution_pct` / `tier2_employee_contribution_pct` fields. Retrofit disclosed Tier 2 rates into those fields when source review confirms them, for consistency with Ghana's multi-tier representation.
